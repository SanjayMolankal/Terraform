name: Terrascan Scan

on:
  push:
    branches: master

jobs:
  terrascan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Run Terrascan
        run: npx ts-node terrascan.ts
